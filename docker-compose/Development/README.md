# Development

## Create credentials

- Semaphore encryption key :
  - run the command : `head -c32 /dev/urandom | base64`

## Logging in / Accounts

- Semaphore :
  - Use the semaphore credentials
  