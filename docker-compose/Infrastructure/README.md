# Infrastructure

Made for centralizing the sockets proxies and shared infrastrucure / management services

## Autoheal

Use the label `autoheal=true` on your containers to enable autoheal on them.

/!\ The containers must have healthchecks configured for autoheal to work properly.
